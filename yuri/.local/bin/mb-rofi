#!/usr/bin/env bash

pkill rofi || true

case "$1" in
c | clipboard)
  cliphist list | rofi -dmenu -no-show-icons -display-columns 2 -p ' ' | cliphist decode | wl-copy
  ;;
e | emoji)
  rofimoji -a copy --use-icons -r '󰞅 '
  ;;
calc | calculator)
  rofi -show calc -modi calc -no-show-match -no-sort
  ;;
-h | --help | h | help)
  echo "Usage: ${0##*/} [ clipboard | emoji | calc | help ]"
  ;;
*)
  rofi -show drun
  ;;
esac

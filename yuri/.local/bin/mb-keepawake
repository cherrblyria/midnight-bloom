#!/usr/bin/env bash

case $1 in
g)
  if pgrep -x "swayidle" >/dev/null; then
    echo ""
  else
    echo "󰒳"
  fi
  ;;
t)
  if pgrep -x "swayidle" >/dev/null; then
    pkill swayidle
    mb-osd "Keep Awake" "on"
  else
    swayidle 2>&1 & disown
    mb-osd "Keep Awake" "off"
  fi
  ;;
esac
